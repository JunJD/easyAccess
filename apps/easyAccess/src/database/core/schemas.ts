
export const dbSchemaV1 = {
  // customFields: '&id, basicId, icon, name, value, createdAt, updatedAt',

  // // item 为序列化json 
  // sections: '&id, builderId, name, columns, separateLinks, visible, content, items, createdAt, updatedAt',
  // basics: '&id, builderId, name, headline, email, phone, picture, location, url, createdAt, updatedAt',

  // metadata 为序列化json
  builders: '&id, title, visibility, locked, data, createdAt, updatedAt',
};